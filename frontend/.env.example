# Postgres connection string (Supabase / Railway / local)
DATABASE_URL="postgresql://user:password@localhost:5432/clearvision?schema=public"

# URL of the FastAPI model-serving backend (see /backend)
NEXT_PUBLIC_MODEL_API_URL="http://localhost:8000"
